@model Usuario
@{
    ViewData["Title"] = "Cadastro de Usuário";
    }

<div id="cadastroUser" class="forms">
    <h2>Cadastro de Usuário</h2>
    <form asp-action="Cadastro" method="POST">
        <p>
            <label asp-for="NomeUser">Nome</label>
            <input asp-for="NomeUser" name="NomeUser" type="text" placeholder="Nome do Usuário" required>
        </p>
        <p>
            <label asp-for="DataNascimento">Data de Nascimento</label>
            <input asp-for="DataNascimento" type="date" name="DataNascimento" required/>
        </p>
        <p>
            <label asp-for="Login">Login</label>
            <input asp-for="Login" name="Login" type="text" required>
        </p>
        <p>
            <label asp-for="Senha">Senha</label>
            <input asp-for="Senha" name="Senha" type="password" required>
        </p>
        <p>
            <label asp-for="Tipo">Tipo de Usuário:</label>
            <select name="Tipo" asp-for="Tipo">
                <option value="0">Administrador</option>
                <option value="1">Colaborador</option>
            </select>
        </p>
        <p>
            <input id="button" type="submit" valor="Cadastrar">
        </p>
        <p id="Mensagem">@ViewBag.Mensagem</p>
    </form>
</div>